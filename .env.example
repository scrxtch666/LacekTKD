# Přihlašovací údaje pro `db` (MySQL) službu v docker-compose.yml.
# Zkopíruj do .env a doplň skutečné hodnoty — docker-compose.yml je
# použije jak pro kontejner s MySQL, tak (přepsáním) pro backend.
DB_ROOT_PASSWORD=
DB_NAME=lacektkd
DB_USER=lacektkd
DB_PASS=
